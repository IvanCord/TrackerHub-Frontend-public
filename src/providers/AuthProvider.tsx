// import React, {useState, createContext} from "react";
// import { autos, motos, blockers } from '../utils/dataDummy'

// const initialStates = {
//     autos, motos, blockers
// }

// export const AuthContext = createContext(initialStates);

// type Props = {
//     children: JSX.Element
// }

// export default function AuthProvider({children}: Props) {
//     // const [globalStates, setGlobalStates] = useState(initialStates);

//     return (
//         // <AuthContext.Provider value={{globalStates, setGlobalStates}}>
//         <AuthContext.Provider value={{initialStates}}>
//             {children}
//         </AuthContext.Provider>
//     )
// }
export {}
